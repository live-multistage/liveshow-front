// The guard itself moved to @live-show/api-contracts (M4a) so the mobile app
// shares it; this module stays as the import path the components already use.
export { isSafeNotificationLink } from '@live-show/api-contracts';
